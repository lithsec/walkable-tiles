node_modules/
bake-*.log
hashes-*.json
out/
*.osm.pbf
*.poly
.DS_Store

# Local multi-slice bake driver output (transient).
states-bake.log
bake-*.log
.bake-state/
out-trial/
# bake-states.sh --trial <slice> writes out-trial-<slice>, not out-trial. Tens of MB, and
# untracked it is one `git add -A` away from being committed.
out-trial-*/
out/
